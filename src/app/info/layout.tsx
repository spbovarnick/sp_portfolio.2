import React from "react"


export default async function InfoLayout({
  children,
}: {
  children: React.ReactNode
}){
  // const infoContent:


  return (
    <div>
      {children}
    </div>
  )
}